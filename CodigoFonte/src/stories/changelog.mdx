import { Meta } from '@storybook/addon-docs/blocks';
import { marked } from 'marked';
import changelog from '../../CHANGELOG.md?raw';

<Meta title="Guideline/Changelog" />


<div dangerouslySetInnerHTML={{ __html: marked.parse(changelog) }} />